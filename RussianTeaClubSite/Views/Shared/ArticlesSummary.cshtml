@model RussianTeaClub.Domain.Entities.Article

<div class="well">
    <h3>
        <strong>@Model.Name</strong>
    </h3>
    <span class="lead">@Model.Description</span>
    <span class="lead">@Model.Description</span>
    @foreach (var t in Model.Tags)
    {
        <a>@t.Name.ToLower()</a>
    }
</div>